# Backend Requirements for PolicyEngine Flow Chart API
flask>=2.3.0
flask-cors>=4.0.0
pyyaml>=6.0
requests>=2.31.0

# Note: PolicyEngine data is loaded from git clones in Railway
# See railway.toml for clone commands